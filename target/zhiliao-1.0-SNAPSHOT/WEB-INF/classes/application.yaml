spring:   #数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3307/zhiliao?serverTimezone=UTC
mybatis:  #mybatis配置
mapper-locations: mapper/*.xml
logging:  #日志配置
  level:
    com:
      book:
        app:
          dao: debug